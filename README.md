# Store Pro AI Customer
is a chatbot designed for small general shops to deal with regular basic queries like types of products, variants, prices

# How to run the application
```
git clone https://github.com/Shoaib1345/StoreProAICustomer.git
```

Setup & Run
# 1️⃣ Create virtual environment
python -m venv venv
venv\Scripts\activate      # Windows
# 2️⃣ Install dependencies
```
pip install flask
```
```
pip install sentence-transformers
pip install requirement
```


``` 
pip install numpy
```
``` 
pip install scikit-learn
```
``` 
pip install datasets
```
``` 
pip install transformers[torch]
```
,,,,
## Before Running the app make sure to finetune the SBERT, for that you can run:

```
python train.py
```
# 3️⃣ Run the Flask app
```
python app.py
```

Then open in browser:
👉 http://127.0.0.1:5000

# Feel free to use and contribute, your contribution means a lot!
- Fork the repo
- Make contribution
- Open pull request and wait for the approval
- Make sure to mention [Ahtisham-1214](https://github.com/Ahtisham-1214) for fast approval after making a pull request
